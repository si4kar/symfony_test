{% extends '@App/layout.html.twig' %}

{% block title %}Category{% endblock %}
{% block body %}
    {% for category in categories %}
        <a href="{{ path('category_item', {id: category.id}) }}">Category {{ category.id }}: {{ category.name }} </a><br>
    {% endfor %}
{% endblock %}

{% block stylesheets %} {% endblock %}
